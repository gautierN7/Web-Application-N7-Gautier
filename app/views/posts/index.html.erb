<h1>Posts#index</h1>
<p>Find me in app/views/posts/index.html.erb</p>
<p style="color : green"> <%= flash[:notice] %></p>

<ul id='posts'>
  <% @posts.each do |post| %>
  <div class='post'>
    <li id='post_<%= post.id %>'>        
        <h2 class='post-title'><%= link_to h(post.title), post, :method => 'get' %></h2> 
	    <div class='post-etat'>
		    <div class='post-info'>posté le <%= post.created_at %></div>
	            <div class='post-nagivation'>        
                    <label class='droite'><%= link_to "Delete this Post", post, { :method => 'delete', :class => 'supprimerp', :title => 'Delete Post' } %></label>
                    <label class='droite'><%= link_to "Edit this Post", edit_post_path(post.id), { :method => 'get', :class => 'editerp', :title => 'Edit Post' } %></label>
                    <label class='droite'><%= link_to "Show this Post", post, { :method => 'get', :class => 'afficherp', :title => 'Show Post' }%></label>
                </div>
         </div>
         <div class='post-body'><%= post.body %></div>
	     <div class='post-commentaire'>
            <% nb_comment = Comment.find_all_by_post_id(post.id).size%>
            <%=nb_comment%> commentaire<%if nb_comment>1%>s<%end%><i></label>
	     </div>					

    </li>
  </div>
  <% end %>
</ul>
<%= link_to "Create a new Post", new_post_path, :class => "createnewpost round" %>

